[project]
name = "build-small-2026"
version = "0.0.1"
description = "Local-AI hackathon entries: Plain-Paper document explainer + Thousand Token Wood"
requires-python = ">=3.11"
dependencies = [
    "gradio>=4.44",
    "requests>=2.31",
    "pillow>=10.0",
]

[project.optional-dependencies]
space = ["llama-cpp-python>=0.3.2"]

[tool.setuptools]
packages = ["engine"]
